# OpenAI Configuration
# Required if using --llm openai (default)
OPENAI_API_KEY=sk-proj-...

# AWS Bedrock Configuration
# Required if using --llm bedrock
# If not set, checks ~/.aws/credentials default profile
AWS_ACCESS_KEY_ID=AKIA...
AWS_SECRET_ACCESS_KEY=wJalr...
# AWS_REGION=us-east-1 # Optional, defaults to us-east-1

# Grok (xAI) Configuration
# Required if using --llm grok
GROK_API_KEY=xai-...

# Private Package Registry Tokens
# Used if configured in packages/knowledge-base/private-packages.json
NPM_MYCOMPANY_TOKEN=npm_...
NPM_INTERNAL_TOKEN=npm_...

# General Configuration
DEBUG=true
